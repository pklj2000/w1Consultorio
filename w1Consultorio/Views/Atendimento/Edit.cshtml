@model w1Consultorio.Models.Atendimento

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Atendimento</legend>

        @Html.HiddenFor(model => model.codAtendimento)

        <div class="editor-label">
            @Html.LabelFor(model => model.codFuncionario)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.codFuncionario)
            @Html.ValidationMessageFor(model => model.codFuncionario)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.codTipoExame)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.codTipoExame)
            @Html.ValidationMessageFor(model => model.codTipoExame)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
