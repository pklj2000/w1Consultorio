@model IEnumerable<w1Consultorio.Models.TipoExame>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h3>Tipos de Exames</h3>

<p>
<input type="button" class="btn btn-primary" value="Novo Tipo de Exame" onclick="location.href='@Url.Action("Create", "TipoExame")'" />
</p>
<table class="table table stripped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
          <div class="btn-group">
            <input type="button" class="btn btn-default" value="Editar" onclick="location.href='@Url.Action("Edit", "TipoExame", new { id = item.CodTipoExame })'" />
            <input type="button" class="btn btn-default" value="Excluir" onclick="location.href='@Url.Action("Delete", "TipoExame", new { id = item.CodTipoExame })'" />
          </div>
        </td>
    </tr>
}

</table>
